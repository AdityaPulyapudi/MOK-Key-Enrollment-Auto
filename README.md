# MOK-Key-Enrollment-Auto
MOK ( Machine Overrride Key ) enrollment is a task where you need to import the keys and enroll them during the Grub boot. I have created an expect automation script that will enter the key strokes. POC done on redhat Open shift VM
